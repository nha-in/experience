{% autoescape off %}Hello {{ organisation.name }},

Sandbox credentials are ready for {{ product.name }} ({{ product.sandbox_id }}). Open the product's Credentials page to reveal the client secret and set your callback and bridge URLs. The sandbox holds synthetic data only.

Sign in: {{ portal_url }}
{% endautoescape %}
